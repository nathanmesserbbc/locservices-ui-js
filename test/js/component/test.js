/* globals define, describe, it */
define(['locservices/ui/component/search', 'locservices/core/api'], function(Search, API) {

  describe('just checking', function() {

    it('works for app', function() {
      expect(true).toBe(false);
    });

  });

});
